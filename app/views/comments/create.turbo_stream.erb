<%= turbo_stream.prepend "#{dom_id(@meow)}_comments" do %>
  <%= render "comment", comment: @comment %>
<% end %>

<%= turbo_stream.replace "#{dom_id(@meow)}_comments_count" do %>
  <%= render "meows/comments_count", meow: @meow %>
<% end %>